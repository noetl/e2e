{
  "name": "webhook_hmac_secret",
  "type": "secret",
  "description": "HMAC-SHA256 signing key for the push-ingress webhook E2E (noetl/ai-meta#90 Phase 3). The gateway resolves this alias from the Wallet to verify the X-Signature header. Test value only — no real secret.",
  "tags": ["webhook", "hmac", "ingress", "subscription", "e2e"],
  "data": {
    "secret": "whsec_e2e_test_signing_key_0123456789"
  }
}
