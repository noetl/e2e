{
  "name": "hook_bearer_token",
  "type": "secret",
  "description": "Expected bearer token for the push-ingress bearer-scheme webhook E2E (noetl/ai-meta#90 Phase 3). The gateway resolves this alias from the Wallet to verify the Authorization header. Test value only — no real secret.",
  "tags": ["webhook", "bearer", "ingress", "subscription", "e2e"],
  "data": {
    "secret": "bearer_e2e_test_token_abcdef0123456789"
  }
}
