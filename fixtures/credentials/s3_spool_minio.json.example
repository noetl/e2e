{
  "name": "s3_spool_minio",
  "type": "aws",
  "description": "In-cluster MinIO (S3-compatible) bucket credential for the subscription spool s3-backend E2E (noetl/ai-meta#94) + cross-restart drain proof (noetl/ai-meta#93). Cluster DNS endpoint + the dev throwaway root creds from ops ci/manifests/minio — no real secret.",
  "tags": ["s3", "minio", "spool", "subscription", "e2e"],
  "data": {
    "access_key_id": "minioadmin",
    "secret_access_key": "minioadmin",
    "region": "us-east-1",
    "endpoint": "http://minio.minio.svc.cluster.local:9000"
  }
}
