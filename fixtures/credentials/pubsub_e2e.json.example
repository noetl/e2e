{
  "name": "pubsub_e2e",
  "type": "pubsub",
  "description": "In-cluster Pub/Sub emulator endpoint for the subscription tool E2E (noetl/ai-meta#90 Phase 1). No secret — plaintext emulator, cluster-internal DNS only.",
  "tags": ["pubsub", "subscription", "e2e", "emulator"],
  "data": {
    "endpoint": "pubsub-emulator.pubsub.svc.cluster.local:8085"
  }
}
