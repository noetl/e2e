---
id: batch-execution
title: Batch Execution
---

Playbooks in `batch_execution`.

| Catalog path | Fixture file | Description | Tools |
| --- | --- | --- | --- |
| `batch_execution/multi_playbook_batch` | `fixtures/playbooks/batch_execution/multi_playbook_batch/multi_playbook_batch.yaml` | - | duckdb, playbook, python |
| `fixtures/playbooks/batch_execution/heavy_payload_pipeline_chunk_worker` | `fixtures/playbooks/batch_execution/heavy_payload_pipeline_chunk_worker/heavy_payload_pipeline_chunk_worker.yaml` | Process one chunk of IDs and run per-item heavy task-sequence pipeline. | postgres, python |
| `fixtures/playbooks/batch_execution/heavy_payload_pipeline_in_step` | `fixtures/playbooks/batch_execution/heavy_payload_pipeline_in_step/heavy_payload_pipeline_in_step.yaml` | Simulate large per-item pipeline payloads and compare direct stress vs chunked processing. | playbook, postgres, python |
| `fixtures/playbooks/batch_execution/heavy_payload_pipeline_in_step_parallel` | `fixtures/playbooks/batch_execution/heavy_payload_pipeline_in_step_parallel/heavy_payload_pipeline_in_step_parallel.yaml` | Simulate large per-item pipeline payloads with parallel chunk-worker orchestration. | playbook, postgres, python |
| `fixtures/playbooks/batch_execution/kind_playbook_lease_expiry` | `fixtures/playbooks/batch_execution/kind_playbook_lease_expiry/kind_playbook_lease_expiry.yaml` | Regression test for NATS lease (120s) expiry in kind:playbook dispatch loops. Uses two combined mechanisms to guarantee lease expiry → duplicate batch workers.  Bug summary:   When a loop step uses `kind: playbook` with `exec: distributed` and `max_in_flight: 1`,   the parent worker holds a NATS consumer lease (120s) while the child runs. If the   child takes &gt;120s (due to API latency, NATS queue placement, NoETL sync overhead),   OR the server takes &gt;120s to dispatch (due to large render_context under concurrent   load), the lease expires → call.error → command.failed → parent execution FAILS.   The stale command is reclaimed by another worker, spawning a DUPLICATE child   execution for the same batch offset → ON CONFLICT DO UPDATE → count doesn't grow.  Mechanism 1 — large execution context (~900KB):   Phase 1 (ctx_step_01..10) builds 6 × 5000-row patient arrays (~150KB each).   Without ctx clearing (our fix), these remain in render_context when   run_batch_workers dispatches. Server must JSON-deserialize + Jinja2-render ~900KB   per dispatch. Under concurrent load (many workers running simultaneously) this   makes each dispatch take 2-5s CPU. With many batches queued, some exceed 120s.  Mechanism 2 — long-running worker (simulate_api_delay: 130s):   Each chunk worker sleeps 130s before processing items, simulating the real MDS   bottleneck: external API response latency + NATS queue placement + NoETL sync.   With max_in_flight: 1 the parent holds its lease while the child runs.   130s sleep guarantees the 120s lease expires for every batch.  Both mechanisms fire simultaneously → reliable lease expiry → duplicate workers.  Bug condition (without ctx_step_09 set_ctx clearing):   render_context ~900KB + worker 130s sleep → command.failed on every batch   → stress_test_results count never grows (ON CONFLICT DO UPDATE overwrites)  Fix condition (with ctx_step_09 set_ctx clearing):   render_context ~20KB → fast dispatch. Worker still sleeps 130s, so the fix   must also address lease timeout for long-running children (increase NATS   consumer ack_wait, or switch dispatch pattern).  Expected pass: ALL batches complete, loop.done fires, summarize step runs. Expected fail (bug): command.failed on batches, execution ends before summarize.  | playbook, postgres, python |
| `fixtures/playbooks/batch_execution/kind_playbook_lease_expiry_worker` | `fixtures/playbooks/batch_execution/kind_playbook_lease_expiry_worker/kind_playbook_lease_expiry_worker.yaml` | Chunk worker for lease expiry regression test (combined trigger approach).  Two mechanisms together guarantee NATS lease expiry → duplicate batch workers:  Mechanism 1 — large parent context (~900KB):   Parent playbook builds 6 × 5000-row ctx arrays before dispatching workers.   Server must JSON-deserialize + Jinja2-render ~900KB per dispatch call.   Under concurrent load this makes each dispatch take 2-5s CPU, so with   many batches queued simultaneously, some dispatches exceed the 120s lease.  Mechanism 2 — long-running worker (simulate_api_delay step):   Each worker sleeps 130s before processing items, simulating the real MDS   bottleneck: external API response latency + NATS queue placement +   NoETL synchronization overhead. With max_in_flight: 1 in the parent, the   parent holds its NATS command lease while waiting for this worker. The   130s sleep guarantees the 120s lease expires for every single batch,   causing command.failed → reclaim → duplicate chunk worker for same offset.  Combined effect:   Both mechanisms fire simultaneously. The parent's run_batch_workers loop   dispatches a child, the child sleeps 130s, the parent's lease expires,   the command is reclaimed, a new child is spawned for the same batch offset.   The duplicate child upserts the same rows (ON CONFLICT DO UPDATE), so   stress_test_results count never grows beyond one batch's worth of rows.  Bug condition (without ctx_step_09 set_ctx clearing):   Parent context ~900KB + worker 130s sleep → lease expires reliably.  Fix condition (with ctx_step_09 set_ctx clearing):   Parent context ~20KB → dispatch fast. But worker still sleeps 130s,   so the fix must also address the lease timeout for long-running children   (e.g. increase NATS consumer ack_wait, or use a different dispatch pattern).  | postgres, python |
| `fixtures/playbooks/batch_execution/parallel_distributed_claim_regression` | `fixtures/playbooks/batch_execution/parallel_distributed_claim_regression/parallel_distributed_claim_regression.yaml` | Regression for duplicate child execution in distributed loop mode. Runs batch workers with:   - loop.spec.mode: parallel   - loop.spec.max_in_flight: 4   - loop.spec.policy.exec: distributed and verifies each batch offset is executed at most once.  | playbook, postgres, python |
| `fixtures/playbooks/batch_execution/parallel_distributed_claim_regression_worker` | `fixtures/playbooks/batch_execution/parallel_distributed_claim_regression_worker/parallel_distributed_claim_regression_worker.yaml` | Child worker for distributed parallel claim regression. Records one run per batch offset and processes that batch.  | postgres, python |
| `fixtures/playbooks/batch_execution/server_oom_stress_chunk_worker` | `fixtures/playbooks/batch_execution/server_oom_stress_chunk_worker/server_oom_stress_chunk_worker.yaml` | Chunk worker for server OOM stress test. Processes one batch of items with per-item distributed parallelism. Each item generates ~250KB compute result triggering _ref externalization, then persists the COMPLETE result (source item + all 600 computed fields) to Postgres. Pool config on save task controls DB connection pressure per worker pod.  | postgres, python |
| `fixtures/playbooks/batch_execution/server_oom_stress_chunk_worker_v2` | `fixtures/playbooks/batch_execution/server_oom_stress_chunk_worker_v2/server_oom_stress_chunk_worker_v2.yaml` | Chunk worker v2 for server OOM stress test. Processes a full batch in a single SQL upsert statement:   - reads the batch window with OFFSET/LIMIT   - builds full result_data (item_id, group_id, payload, status, 600 fields)   - upserts all rows in one DB command This variant removes per-item distributed subcommands to compare throughput.  | postgres, python |
| `fixtures/playbooks/batch_execution/server_oom_stress_test` | `fixtures/playbooks/batch_execution/server_oom_stress_test/server_oom_stress_test.yaml` | Stress test for server/worker communication under large-context batch processing. Validates that server-worker communication handles big data without degradation:   - 21 context steps (~3-8KB each) to simulate fat execution context (42 vars in prod)   - 3000 items dispatched across chunk workers (serial by default)   - Each item produces a ~250KB compute result (matching MDS assessment JSON payloads)     which triggers _ref externalization (&gt; 64KB NOETL_INLINE_MAX_BYTES threshold)   - Per-item distributed parallel processing exercises: NATS dispatch, claim protocol,     adaptive concurrency controller, event pipeline, and DB pool under controlled contention   - No sensitive data, no external HTTP calls What this test validates:   - Concurrent worker claims at scale (up to 6 simultaneous NATS commands)   - Adaptive concurrency controller ramp-up and stabilization under load   - Server DB pool handling parallel event ingestion from multiple workers   - Postgres pool pressure remains within deployed max_size (4 per worker)   - No data loss: all 3000 items processed correctly under concurrent load  | playbook, postgres, python |
| `fixtures/playbooks/batch_execution/server_oom_stress_test_v2` | `fixtures/playbooks/batch_execution/server_oom_stress_test_v2/server_oom_stress_test_v2.yaml` | Stress test v2 for server/worker communication under large-context batch processing. Validates that server-worker communication handles big data without degradation:   - 21 context steps (~3-8KB each) to simulate fat execution context (42 vars in prod)   - 3000 items dispatched across chunk workers (serial by default)   - Each item persists full result_data (source payload + 600 computed fields)     generated in SQL within chunk worker batch upserts   - Per-item distributed parallel processing exercises: NATS dispatch, claim protocol,     adaptive concurrency controller, event pipeline, and DB pool under controlled contention   - No sensitive data, no external HTTP calls What this test validates:   - Concurrent worker claims at scale (up to 6 simultaneous NATS commands)   - Adaptive concurrency controller ramp-up and stabilization under load   - Server DB pool handling parallel event ingestion from multiple workers   - Postgres pool pressure remains within deployed max_size (4 per worker)   - No data loss: all 3000 items processed correctly under concurrent load  | playbook, postgres, python |
| `fixtures/playbooks/batch_execution/traveler_batch_enrichment_chunk_worker` | `fixtures/playbooks/batch_execution/traveler_batch_enrichment_chunk_worker/traveler_batch_enrichment_chunk_worker.yaml` | Process one traveler batch window. Fetch rows, run parallel HTTP enrichment, transform, and store. | http, postgres, python |
| `fixtures/playbooks/batch_execution/traveler_batch_enrichment_in_step` | `fixtures/playbooks/batch_execution/traveler_batch_enrichment_in_step/traveler_batch_enrichment_in_step.yaml` | Query travelers in 100-row batches and process each batch with parallel HTTP enrichment. | playbook, postgres, python |
