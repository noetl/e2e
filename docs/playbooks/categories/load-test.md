---
id: load-test
title: Load Test
---

Playbooks in `load_test`.

| Catalog path | Fixture file | Description | Tools |
| --- | --- | --- | --- |
| `fixtures/playbooks/load_test/tooling_non_blocking` | `fixtures/playbooks/load_test/tooling_non_blocking/tooling_non_blocking.yaml` | Validate that representative tools do not serialize execution under parallel loop load. Core probes (HTTP/Postgres/DuckDB) run by default. Optional probes (Snowflake, NATS KV) can be enabled via workload flags.  Phase 0 RisingWave alignment removed the NATS Object Store probe (`run_nats_object_probe`) along with the `object` storage tier. The disk-cache-backed large-payload path will be covered by a dedicated probe once phase 1 lands the DiskCacheBackend.  | duckdb, http, nats, postgres, python, snowflake |
| `load_test/heavy_loop_aggregation` | `fixtures/playbooks/load_test/heavy_loop_aggregation/heavy_loop_aggregation.yaml` | Heavy load test for loop handling and result aggregation.  This playbook tests the NATS K/V refactoring by: 1. Processing a large number of items (configurable, default 100) 2. Each iteration produces a result with meaningful data 3. Results are aggregated at the end 4. Validates that NATS K/V stores only counts, not results  The test should complete successfully without hitting NATS 1MB limit because results are stored in event table, not NATS K/V.  | python |
| `load_test/reference_chain_loop_stress` | `fixtures/playbooks/load_test/reference_chain_loop_stress/reference_chain_loop_stress.yaml` | Neutral end-to-end stress test for pagination + loop execution.  Streaming design goals: 1. Exercise high-volume paginated fetch (hundreds/thousands of records). 2. Keep only bounded linked references in API responses/context. 3. Process each page independently to avoid unbounded context growth. 4. Validate completion counts and linked-chain contract.  | noop, python |
| `load_test/state_report_synthetic_load` | `fixtures/playbooks/load_test/state_report_synthetic_load/state_report_synthetic_load.yaml` | Synthetic load test for a state-report generation workflow. Uses synthetic facility and patient data but keeps the production-shaped control flow and wide context characteristics:   - 21 context steps (~3-8KB each) to simulate fat execution context   - 540 synthetic patient work items dispatched across chunk workers (serial by default)   - Each item persists full result_data (source payload + 600 computed fields)     generated in SQL within chunk worker batch upserts   - Per-batch distributed parallel processing exercises: NATS dispatch, claim protocol,     adaptive concurrency controller, event pipeline, and DB pool under controlled contention   - No sensitive data, no external HTTP calls What this test validates:   - Concurrent worker claims at scale   - Adaptive concurrency controller ramp-up and stabilization under load   - Server DB pool handling parallel event ingestion from multiple workers   - Postgres pool pressure remains within deployed limits   - No data loss: all synthetic patient work items processed correctly under concurrent load  | playbook, postgres, python |
| `load_test/state_report_synthetic_load/state_report_synthetic_load_worker` | `fixtures/playbooks/load_test/state_report_synthetic_load/state_report_synthetic_load_worker.yaml` | Synthetic chunk worker for the state-report load test. Processes a full batch in a single SQL upsert statement:   - reads the batch window with OFFSET/LIMIT   - builds full result_data (item_id, group_id, payload, status, 600 fields)   - upserts all rows in one DB command This variant removes per-item distributed subcommands to compare throughput.  | postgres, python |
